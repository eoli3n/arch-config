# ZFS
- zfs hibernate without swap ?
- smart test timer ?

# DNS
- fix DNS per interface ? as https://github.com/jonathanio/update-systemd-resolved#preventing-leakage-to-corporate-networks
- fix DNS to 1.1.1.1 and fallback to 9.9.9.9

# FIX
ipxe-netboot aur package broken

# NETWORK
- try to remove ip forward on enX.network and change wlp2s0.network to IPForward=yes: test containers networking

# SERVICES
- hardware acceleration packages
- disable ipv6

# ISSUES
- virtio-gpu : https://bbs.archlinux.org/viewtopic.php?pid=1901266#p1901266
- RW snapshots with snapper : https://github.com/openSUSE/snapper/issues/535

# IMPROVMENTS
- notify-send : scrub, trim, and backup : root cannot send send notification to user
- move max aur package to community equivalents

# TESTS
- zfs trim + zfs autoscrub systemd timer
- libvirt zfs pool storage : how to use a subdataset ?
- docker with zfs pool storage
- arch-audit hook
- reflector service at startup
- restest brightnest with light instead of light-git
